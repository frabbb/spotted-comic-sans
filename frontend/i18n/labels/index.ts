// import labelsQuery from "@/graphql/queries/sanity/singles/labels";

export default defineI18nLocale(async (locale) => {
  // const config = useRuntimeConfig();
  // const url = config.public.sanityApi;

  // const { data } = await $fetch(url, {
  //   method: "POST",
  //   body: {
  //     query: labelsQuery,
  //   },
  // });

  // const labels = data?.allLabels[0]?.labels?.reduce((reducer, { key, value }) => {
  //   return {
  //     ...reducer,
  //     [key]: value,
  //   };
  // }, {});

  // return labels || {};
  return {};
});
